/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // Ensure static files in root don't override Next.js routes
  // Static HTML files have been moved to /legacy folder
};

module.exports = nextConfig;

